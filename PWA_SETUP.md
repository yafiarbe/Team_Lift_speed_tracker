# PWA Setup (Progressive Web App)

## ✅ Что сделано

Приложение настроено как **Progressive Web App (PWA)** и может быть установлено как приложение на мобильных устройствах.

### Установленные компоненты:

- ✅ `@vite-pwa/nuxt` - модуль PWA для Nuxt
- ✅ Service Worker - для кэширования и офлайн работы
- ✅ Web Manifest - конфигурация приложения
- ✅ PWA иконки (192x192, 512x512)
- ✅ Meta теги для iOS поддержки

## 🚀 Как установить на мобильное устройство

### На Android (Chrome):

1. Откройте приложение в браузере Chrome
2. Нажмите на три точки (меню) → **"Установить приложение"**
3. Приложение появится на главном экране

### На iOS (Safari):

1. Откройте приложение в Safari
2. Нажмите на кнопку **Поделиться**
3. Выберите **"На экран Домой"**
4. Приложение появится на главном экране

## 📋 Возможности PWA

- ✅ **Установка на главный экран** - без необходимости App Store
- ✅ **Офлайн работа** - Service Worker кэширует данные
- ✅ **Своя иконка** - отображается на экране как отдельное приложение
- ✅ **Полноэкранный режим** - no address bar, no tabs
- ✅ **Кэширование API** - сохраняет данные на 5 минут

## 📁 Файлы конфигурации

- `nuxt.config.ts` - основная конфигурация PWA
- `public/pwa-192x192.png` - иконка для мобильных устройств
- `public/pwa-512x512.png` - иконка для сплеша
- `.output/public/sw.js` - Service Worker (генерируется при сборке)
- `.output/public/manifest.json` - Web Manifest (генерируется при сборке)

## 🔧 Запуск production сборки локально

```bash
npm run build
node .output/server/index.mjs
```

Потом откройте `http://localhost:3000` в браузере.

## 📱 Развертывание

Для развертывания на сервер (Vercel, Netlify, etc.):

```bash
npm run build
# Скопировать папку .output на хостинг
```

После развертывания:

1. Пользователи откроют ваше приложение в браузере
2. Увидят кнопку "Установить" (на Android) или опцию в меню (на iOS)
3. После установки приложение будет работать как native app

## 🎨 Кастомизация

Если хотите изменить иконку, название или цвета - отредактируйте `nuxt.config.ts`:

```typescript
pwa: {
  manifest: {
    name: "Новое имя",
    short_name: "Короткое имя",
    theme_color: "#НОВЫЙ_ЦВЕТ",
  }
}
```

Потом пересоберите: `npm run build`
